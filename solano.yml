ruby_version: ruby-2.3.1
bundler_version: 1.12.2

test_pattern:
  - spec/**/*_spec.rb

rake:
  rails_env: test

postgresql:
  version: '9.3'

firefox: '31.0esr'

#cp tmp/capybara/screenshot_edit\ and\ save\ \ valid\ task.png $HOME/results/$TDDIUM_SESSION_ID/session/
hooks:
  package_setup: "rbenv local 2.3.1 && gem install bundler && bundle install"
  post_worker: "cat ~/excon.log"

collect:
  repo:
    - "without-spaces.txt"
    - "with one-space.txt"
    - "excon.log"
#    - "with two  spaces.txt"

environment:
  'EXCON_DEBUG': 'true'
  'DEBUG': 'true'
