ruby_version: ruby-2.3.1
bundler_version: 1.12.2

postgresql:
  version: '9.3'

firefox: '31.0esr'

notify:
- channel: hipchat
  trigger-on: failed
  branch: mas*er
  room: sandbox
  message: "pr/1"

profiles:
  a:
    tests:
      - "exit 0"
    test_pattern:
      - spec/**_test.rb
  b:
    tests:
      - "exit 0"
    test_pattern:
      - spec/**_test.rb
pipeline:
  a:
    - b
